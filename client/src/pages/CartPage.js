import CartContainer from "../components/CartContainer";
import './CartPage.css';

const CartPage = () => {
    return (
        <div className="cart-page">
            <CartContainer/>
        </div>
    );
}
 
export default CartPage;